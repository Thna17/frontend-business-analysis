import { LoadingScreen } from "@/components/shared/loading-screen";

// Global loading shell used while Next.js streams route content.
export default function Loading() {
  return <LoadingScreen />;
}
